package com.kellydwalters.beertome;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class BeerDescription extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_beer_description);

        //show beer based on id
        // have an edit button that brings up a new activity to edit/enter a new beer
    }
}
